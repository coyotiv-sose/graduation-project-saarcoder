class Offer {
  constructor(creator, listing, start, end, price) {
    this.creator = creator
    this.listing = listing
    this.start = start
    this.end = end
    this.minPrice = price
  }
}
module.exports = Offer
